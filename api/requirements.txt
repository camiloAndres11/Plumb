fastapi>=0.115,<1
uvicorn[standard]>=0.32,<1
pydantic>=2.9,<3
pydantic-settings>=2.6,<3
# pool: la API publica hace varias consultas por request y abrir una
# conexion TCP nueva en cada una es el cuello de botella obvio.
psycopg[binary,pool]>=3.2,<4
anthropic>=0.68,<1
# servidor MCP (api/app/mcp/). Lee de Postgres (psycopg, arriba), no de
# archivos locales. Ver MCP.md.
mcp>=2.0,<3
